package com.example.media; // Define the package for the EnhancedMedia interface

// Define the EnhancedMedia interface which extends the Media interface
public interface EnhancedMedia extends Media {
    // Method to pause the media
    void pause();

    // Method to rewind the media
    void rewind();
}
